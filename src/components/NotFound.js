import React from 'react';

const NotFound = () => {
    return(
        <li className="not-found">
            <p>We couldn't find anything that matched your search. Sorry!</p>
        </li>
    );
}

export default NotFound;